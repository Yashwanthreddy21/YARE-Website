'use client';import Link from'next/link';import{useAppStore}from'@/store/useAppStore';
export default function GoalsPage(){const tasks=useAppStore(s=>s.tasks);const goals=tasks.filter(t=>t.targetValue||t.minValue||t.maxValue);return <div><p className="text-sm text-slate-500">Overall targets</p><h1 className="mb-6 text-3xl font-black">Goals</h1><div className="space-y-2">{goals.map(t=><Link href={`/tasks/${t.id}`} className="card flex items-center justify-between p-4" key={t.id}><div><b>{t.name}</b><div className="text-xs text-slate-500">Tap to edit goal</div></div><b>{t.targetValue??`${t.minValue}–${t.maxValue}`} {t.unit}</b></Link>)}</div></div>}
