'use client';
import{useState}from'react';
const initial=[['06:00','Wake Up'],['06:30','Gym / Workout'],['08:00','Breakfast'],['08:30','Job Applications'],['11:00','Preparation / Study'],['13:00','Lunch'],['14:00','Job Applications'],['16:00','Interview Prep / Learning'],['18:00','Steps / Walk'],['19:30','Dinner'],['20:30','Daily Review'],['21:30','Prepare for Bed']];
export default function SchedulePage(){const[items,setItems]=useState(initial);return <div><p className="text-sm text-slate-500">Your editable day</p><h1 className="mb-6 text-3xl font-black">Routine</h1><div className="space-y-2">{items.map((x,i)=><div className="card flex items-center gap-3 p-3" key={i}><input className="input max-w-32" type="time" value={x[0]} onChange={e=>setItems(s=>s.map((v,j)=>j===i?[e.target.value,v[1]]:v))}/><input className="input" value={x[1]} onChange={e=>setItems(s=>s.map((v,j)=>j===i?[v[0],e.target.value]:v))}/><button className="btn-secondary px-3" onClick={()=>setItems(s=>s.filter((_,j)=>j!==i))}>×</button></div>)}</div><button className="btn-primary mt-4" onClick={()=>setItems(s=>[...s,['12:00','New schedule item']])}>Add schedule item</button><p className="mt-3 text-xs text-slate-500">Schedule editing works in this MVP. Per-day schedules and cloud persistence are included in the Supabase schema for the next sync step.</p></div>}
