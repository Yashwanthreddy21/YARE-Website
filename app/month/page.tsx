'use client';
import { useState } from 'react';
import { addMonths, eachDayOfInterval, endOfMonth, format, startOfMonth, subMonths } from 'date-fns';
import { ChevronLeft, ChevronRight } from 'lucide-react';
import { useAppStore } from '@/store/useAppStore';
import { dailyScore } from '@/utils/progress';
export default function MonthPage(){const [cursor,setCursor]=useState(new Date());const {tasks,logs}=useAppStore();const start=startOfMonth(cursor),end=endOfMonth(cursor),days=eachDayOfInterval({start,end});const blanks=Array(start.getDay()).fill(null);return <div><div className="mb-6 flex items-center justify-between"><div><p className="text-sm text-slate-500">Monthly progress</p><h1 className="text-3xl font-black">{format(cursor,'MMMM yyyy')}</h1></div><div className="flex gap-1"><button className="btn-secondary px-3" onClick={()=>setCursor(subMonths(cursor,1))}><ChevronLeft/></button><button className="btn-secondary px-3" onClick={()=>setCursor(new Date())}>Today</button><button className="btn-secondary px-3" onClick={()=>setCursor(addMonths(cursor,1))}><ChevronRight/></button></div></div><div className="card overflow-hidden p-3"><div className="grid grid-cols-7 text-center text-xs font-bold text-slate-400">{['Sun','Mon','Tue','Wed','Thu','Fri','Sat'].map(d=><div className="py-2" key={d}>{d.slice(0,1)}</div>)}</div><div className="grid grid-cols-7 gap-1">{blanks.map((_,i)=><div key={`b${i}`}/>) }{days.map(day=>{const score=dailyScore(tasks,logs,day);const future=day>new Date();return <a href={`/day/${format(day,'yyyy-MM-dd')}`} key={day.toISOString()} className="aspect-square rounded-xl p-1.5 text-center hover:bg-slate-50 dark:hover:bg-slate-800"><div className="text-sm font-semibold">{format(day,'d')}</div><div className={`mx-auto mt-1 grid h-8 w-8 place-items-center rounded-full text-[10px] font-bold ${future?'bg-slate-50 text-slate-300 dark:bg-slate-800':score===100?'bg-emerald-100 text-emerald-700':'bg-slate-100 text-slate-600 dark:bg-slate-800'}`}>{future?'—':`${score}%`}</div></a>})}</div></div></div>}
